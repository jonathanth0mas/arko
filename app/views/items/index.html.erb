<%= form_tag(items_path, method: "get") do %>
	
	<%= select_tag "dish", options_from_collection_for_select(DISH, "to_s", "to_s") %>
	<%= submit_tag "Search", class: "btn btn-success" %>

<% end %>

<h1><%= params[:dish] %></h1>

<% @items.each do |item| %>
	
	<%= render 'shared/item_row', item: item, items_orders: @items_orders %>

<% end %>