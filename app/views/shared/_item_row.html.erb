<div class="col-lg-4 item-list text-center">

	<div>
	<%= item.name %> <%= link_to "Edit", edit_item_path(item.id) %>
	</div>
	<div>
	<img src="#" width="300" height="270">
	</div>

	<%= form_for order_item, remote: true do |f| %>

		<div>
		<span>Qty: </span><%= f.number_field :quantity, value: 1, min: 1 %>
		</div>
		<%= f.hidden_field :item_id, value: item.id %>
		<div>
		<%= f.select :size, options_for_select({"Small (T) - $#{item.smallT}" => "#{item.smallT}", "Medium (T) - $#{item.mediumT}" => "#{item.mediumT}", "Large (T) - $#{item.largeT}" => "#{item.largeT}", "Small (C) - $#{item.smallC}" => "#{item.smallC}", "Medium (C) - $#{item.mediumC}" => "#{item.mediumC}", "Large (C) - $#{item.largeC}" => "#{item.largeC}", "Each - #{item.each}" => "#{item.each}", "Half - #{item.half}" => "#{item.half}", "Whole - #{item.whole}" => "#{item.whole}", "Full - #{item.full}" => "#{item.full}"}) %>
		</div>
		<div>
	 	<%= f.submit "Add to Order", class: "btn btn-primary o-btn" %>
	 	</div>

	<% end %>
</div>